apiVersion: v1
kind: Service
metadata:
  name:  svc-load-balancer-catalogodeprodutos
spec:
  type:  LoadBalancer
  selector:
    app: catalogo-de-produtos-pod
  ports:
    - port: 9000
      targetPort: 9999
      nodePort: 30000